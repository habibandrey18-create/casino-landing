# Casino Landing Page

Лендинг для онлайн-казино с системой вкладок, трекингом событий и админ-панелью для просмотра статистики.

## 🚀 Быстрое развертывание на Render

### Требования
- Node.js 20+
- npm или yarn

### Установка зависимостей
```bash
npm install
```

### Локальный запуск
```bash
npm start
```

Сайт будет доступен на `http://localhost:3000`

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
ADMIN_PASSWORD=ваш_пароль_для_админки
JWT_SECRET=случайная_строка_для_безопасности
PORT=3000
NODE_ENV=production
```

### Развертывание на Render

1. Загрузите проект на GitHub
2. Подключите репозиторий к Render
3. Настройки:
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Environment:** `node`
4. Добавьте переменные окружения в панели Render:
   - `ADMIN_PASSWORD`
   - `JWT_SECRET`
   - `NODE_ENV=production`
   - `PORT=10000`

## 📁 Структура проекта

```
├── index.html          # Основной лендинг
├── admin.html          # Админ-панель
├── server.js           # Node.js сервер (Express + SQLite)
├── casinos-data.js     # Структура данных казино
├── tabs-manager.js     # Управление вкладками
├── tracking.js         # Система трекинга событий
├── package.json        # Зависимости Node.js
└── .env                # Переменные окружения (не в Git)
```

## 🔐 Админ-панель

Доступ: `/admin`

Войдите с паролем, указанным в переменной окружения `ADMIN_PASSWORD`.

## 📊 База данных

SQLite база `analytics.db` создаётся автоматически при первом запуске.

## 🛠️ Технологии

- **Frontend:** HTML5, CSS3, Vanilla JavaScript
- **Backend:** Node.js, Express
- **Database:** SQLite3
- **Authentication:** JWT
- **Security:** express-rate-limit, cookie-parser

## 📝 Лицензия

Приватный проект.
